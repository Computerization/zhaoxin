{
  "name": "zhaoxin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "karabiner": "node scripts/generate-karabiner.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^6.2.4"
  }
}
